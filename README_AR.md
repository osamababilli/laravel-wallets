# نظام إدارة المستخدمين والأدوار والترجمات

## نظرة عامة

هذا المشروع عبارة عن نظام إدارة شامل مبني على Laravel مع Livewire، يوفر إدارة المستخدمين والأدوار والصلاحيات والترجمات متعددة اللغات. النظام مصمم ليكون نقطة انطلاق قوية للتطبيقات التي تحتاج إلى نظام إدارة متقدم.

## الميزات الرئيسية

### 🔐 إدارة المستخدمين والأدوار
- **إدارة المستخدمين**: إنشاء وتعديل وحذف المستخدمين
- **نظام الأدوار والصلاحيات**: إدارة شاملة للأدوار والصلاحيات باستخدام Spatie Permission
- **تتبع الأنشطة**: تسجيل جميع أنشطة المستخدمين باستخدام Activity Log
- **حالة المستخدم**: إمكانية تفعيل/إلغاء تفعيل المستخدمين

### 🌐 نظام الترجمة متعدد اللغات
- **إدارة اللغات**: إضافة وتعديل اللغات المدعومة
- **مدير الترجمة**: واجهة سهلة لإدارة الترجمات
- **دعم RTL/LTR**: دعم كامل للغات من اليمين إلى اليسار
- **تصدير/استيراد الترجمات**: إمكانية تصدير الترجمات إلى ملفات JSON

### 🎨 واجهة المستخدم
- **Livewire Flux**: واجهة مستخدم حديثة وجذابة
- **Tailwind CSS**: تصميم متجاوب وجميل
- **Preline UI**: مكونات واجهة مستخدم متقدمة
- **SweetAlert2**: تنبيهات تفاعلية

## التقنيات المستخدمة

### Backend
- **Laravel 12**: إطار عمل PHP متقدم
- **Livewire**: تطوير واجهات تفاعلية بدون JavaScript
- **Livewire Volt**: مكونات Livewire مبسطة
- **Spatie Permission**: إدارة الأدوار والصلاحيات
- **Spatie Activity Log**: تسجيل أنشطة المستخدمين
- **Laravel Passport**: API Authentication

### Frontend
- **Tailwind CSS 4**: إطار عمل CSS متقدم
- **Livewire Flux**: مكتبة مكونات Livewire
- **Preline UI**: مكونات واجهة مستخدم
- **SweetAlert2**: مكتبة التنبيهات
- **Vite**: أداة البناء السريعة

### Development Tools
- **Pest**: إطار عمل الاختبارات
- **Laravel Pint**: منسق الكود
- **Laravel Debugbar**: أدوات التطوير
- **Laravel Sail**: بيئة التطوير

## متطلبات النظام

- PHP 8.2 أو أحدث
- Composer
- Node.js 18+ و npm
- MySQL/PostgreSQL/SQLite
- Git

## التثبيت

### 1. استنساخ المشروع
```bash
git clone <repository-url>
cd startar
```

### 2. تثبيت التبعيات
```bash
# تثبيت تبعيات PHP
composer install

# تثبيت تبعيات Node.js
npm install
```

### 3. إعداد البيئة
```bash
# نسخ ملف البيئة
cp .env.example .env

# توليد مفتاح التطبيق
php artisan key:generate

# إعداد قاعدة البيانات في ملف .env
DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=startar
DB_USERNAME=your_username
DB_PASSWORD=your_password
```

### 4. إعداد قاعدة البيانات
```bash
# تشغيل المايجريشن
php artisan migrate

# تشغيل السيدرز الأساسية
php artisan db:seed

# تشغيل سيد الصلاحيات والأدوار (مهم جداً)
php artisan db:seed --class=roles_permission
```

### 5. بناء الأصول
```bash
# بناء الأصول للإنتاج
npm run build

# أو تشغيل في وضع التطوير
npm run dev
```

### 6. تشغيل المشروع
```bash
# تشغيل الخادم
php artisan serve

# أو استخدام Laravel Sail
./vendor/bin/sail up
```

## بيانات الدخول الافتراضية

بعد تشغيل السيدرز، يمكنك الدخول باستخدام:

- **البريد الإلكتروني**: test@example.com
- **كلمة المرور**: password

## هيكل المشروع

```
app/
├── Http/Controllers/          # المتحكمات
├── Livewire/                 # مكونات Livewire
│   ├── Actions/              # الإجراءات
│   ├── Languages/            # إدارة اللغات
│   ├── Logs/                 # سجلات الأنشطة
│   ├── Permissions/          # إدارة الصلاحيات
│   ├── Roles/                # إدارة الأدوار
│   ├── Translations/         # إدارة الترجمات
│   └── Users/                # إدارة المستخدمين
├── Models/                   # النماذج
├── Policies/                 # سياسات الصلاحيات
├── Services/                 # الخدمات
└── Support/                  # الملفات المساعدة

database/
├── migrations/               # ملفات قاعدة البيانات
└── seeders/                  # بيانات البداية

resources/
├── css/                      # ملفات CSS
├── js/                       # ملفات JavaScript
└── views/                    # قوالب Blade
    ├── components/           # المكونات
    ├── livewire/             # قوالب Livewire
    └── flux/                 # قوالب Flux
```

## الميزات المتقدمة

### نظام الترجمة
- **إدارة ديناميكية**: إضافة وتعديل الترجمات من واجهة الإدارة
- **دعم متعدد اللغات**: دعم غير محدود للغات
- **تصدير تلقائي**: تصدير الترجمات إلى ملفات JSON
- **مزامنة ذكية**: مزامنة الترجمات مع ملفات اللغة

### نظام الصلاحيات
- **صلاحيات دقيقة**: تحكم دقيق في الصلاحيات
- **أدوار مرنة**: إنشاء أدوار مخصصة
- **حماية شاملة**: حماية جميع العمليات الحساسة

### تتبع الأنشطة
- **سجل شامل**: تسجيل جميع أنشطة المستخدمين
- **تفاصيل دقيقة**: معلومات مفصلة عن كل نشاط
- **تصفية متقدمة**: إمكانية البحث والتصفية في السجلات

## الدوال المساعدة (Helpers)

يحتوي المشروع على دوال مساعدة مخصصة في ملف `app/Support/helpers.php`:

### دالة الإشعارات `notify()`
```php
/**
 * دالة إشعارات موحدة وبسيطة
 *
 * @param string $message رسالة الإشعار
 * @param string $type نوع الإشعار (success, error, warning, info)
 * @param bool $redirect هل تريد الإشعار يظهر بعد redirect
 */
function notify(string $message, string $type = 'success', bool $redirect = true)
```

**أمثلة الاستخدام:**
```php
// إشعار نجاح
notify('تم الحفظ بنجاح', 'success');

// إشعار خطأ
notify('حدث خطأ في العملية', 'error');

// إشعار تحذير
notify('يرجى التحقق من البيانات', 'warning');

// إشعار معلومات
notify('تم تحديث البيانات', 'info');
```

### دالة تأكيد الحذف `confermeDelete()`
```php
/**
 * دالة تأكيد الحذف مع إرسال إشعار للمستخدم
 *
 * @param object $component مكون Livewire
 * @param string $title عنوان التأكيد
 * @param string $message رسالة التأكيد
 * @param mixed $itemId معرف العنصر المراد حذفه
 */
function confermeDelete($component, $title, $message, $itemId)
```

**مثال الاستخدام:**
```php
// في مكون Livewire
public function delete($id)
{
    confermeDelete($this, 'تأكيد الحذف', 'هل أنت متأكد من حذف هذا العنصر؟', $id);
}
```

## سيد الصلاحيات والأدوار

يحتوي المشروع على سيد شامل لإعداد الصلاحيات والأدوار في `database/seeders/roles_permission.php`:

### الأدوار المُنشأة
- **Super Admin**: مدير النظام الرئيسي مع جميع الصلاحيات

### الصلاحيات المُنشأة

#### صلاحيات إدارة المستخدمين
- `create user` - إنشاء مستخدم جديد
- `edit user` - تعديل بيانات المستخدم
- `delete user` - حذف المستخدم
- `view users` - عرض قائمة المستخدمين
- `show user` - عرض تفاصيل المستخدم

#### صلاحيات إدارة الأدوار
- `create role` - إنشاء دور جديد
- `edit role` - تعديل الدور
- `delete role` - حذف الدور
- `view roles` - عرض قائمة الأدوار

#### صلاحيات إدارة الصلاحيات
- `create permission` - إنشاء صلاحية جديدة
- `edit permission` - تعديل الصلاحية
- `delete permission` - حذف الصلاحية
- `view permissions` - عرض قائمة الصلاحيات

#### صلاحيات سجلات الأنشطة
- `view activity logs` - عرض سجلات الأنشطة

#### صلاحيات إدارة اللغات
- `view languages` - عرض اللغات
- `create language` - إضافة لغة جديدة
- `edit language` - تعديل اللغة
- `delete language` - حذف اللغة
- `set default language` - تعيين اللغة الافتراضية

#### صلاحيات إدارة الترجمات
- `view translations` - عرض الترجمات
- `edit translations` - تعديل الترجمات
- `import translations` - استيراد الترجمات
- `export translations` - تصدير الترجمات
- `publish translations` - نشر الترجمات

### المستخدم الافتراضي
```php
البريد الإلكتروني: test@example.com
كلمة المرور: password
الدور: Super Admin
```

### اللغات المُنشأة
- **الإنجليزية (en)**: اللغة الافتراضية، اتجاه LTR
- **العربية (ar)**: اتجاه RTL، نشطة

### الترجمات المُستوردة
يتم استيراد الترجمات العربية من ملف `database/seeders/ar.json` تلقائياً عند تشغيل السيدر.

## التطوير

### تشغيل الاختبارات
```bash
php artisan test
```

### تنسيق الكود
```bash
./vendor/bin/pint
```

### مراقبة السجلات
```bash
php artisan pail
```

## المساهمة

1. Fork المشروع
2. إنشاء فرع للميزة الجديدة (`git checkout -b feature/amazing-feature`)
3. Commit التغييرات (`git commit -m 'Add some amazing feature'`)
4. Push إلى الفرع (`git push origin feature/amazing-feature`)
5. فتح Pull Request

## الترخيص

هذا المشروع مرخص تحت رخصة MIT. راجع ملف [LICENSE](LICENSE) للتفاصيل.

## الدعم

إذا واجهت أي مشاكل أو لديك أسئلة، يرجى فتح issue في المستودع.

## التحديثات المستقبلية

- [ ] نظام الإشعارات
- [ ] لوحة تحكم متقدمة
- [ ] API RESTful كامل
- [ ] نظام النسخ الاحتياطي
- [ ] دعم الملفات المتعددة
- [ ] نظام التقارير المتقدم

---

**تم تطوير هذا المشروع باستخدام أحدث تقنيات Laravel و Livewire لضمان الأداء والأمان والسهولة في الاستخدام.**
